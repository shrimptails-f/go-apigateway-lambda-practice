# 実AWSのdev環境向け
APP=dev # 語群はci local dev stg prod
GO_PORT=8080
APP_ROOT=/home/dev/backend

AWS_DEFAULT_REGION=ap-northeast-1
CDK_DEFAULT_REGION=${AWS_DEFAULT_REGION}

# AWS SDK用に認証情報を設定※ローカルのみ
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
